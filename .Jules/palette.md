# Palette's Journal

## 2024-05-22 - [Accessibility First Steps]
**Learning:** Static sites without build systems often miss basic accessibility defaults like focus indicators because they rely on browser defaults which may be insufficient or inconsistent with custom designs.
**Action:** Always verify `focus-visible` states on custom buttons and cards, especially when they use `border-radius` or `overflow: hidden`.
